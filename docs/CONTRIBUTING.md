# CONTRIBUTING

We welcome contributions! Please follow our coding standards defined in `.agent/rules/rules.md`.

## Guidelines
- Use `F_Snake_Case` for functions.
- Every file must have `#region` markers.
- Comments are only allowed at the start of functions or after regions.
